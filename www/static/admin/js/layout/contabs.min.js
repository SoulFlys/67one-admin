$(function() {
    function m(a) {
        var b = 0;
        $(a).each(function() {
            b += $(this).outerWidth(true)
        });
        return b
    }
    function l(b) {
        var a = m($(b).prevAll()),
            f = m($(b).nextAll());
        var d = m($(".content-tabs").children().not(".J_menuTabs"));
        var e = $(".content-tabs").outerWidth(true) - d;
        var g = 0;
        if ($(".page-tabs-content").outerWidth() < e) {
            g = 0
        } else {
            if (f <= (e - $(b).outerWidth(true) - $(b).next().outerWidth(true))) {
                if ((e - $(b).next().outerWidth(true)) > f) {
                    g = a;
                    var c = b;
                    while ((g - $(c).outerWidth()) > ($(".page-tabs-content").outerWidth() - e)) {
                        g -= $(c).prev().outerWidth();
                        c = $(c).prev()
                    }
                }
            } else {
                if (a > (e - $(b).outerWidth(true) - $(b).prev().outerWidth(true))) {
                    g = a - $(b).prev().outerWidth(true)
                }
            }
        }
        $(".page-tabs-content").animate({
            marginLeft: 0 - g + "px"
        }, "fast")
    }
    function s() {
        var a = Math.abs(parseInt($(".page-tabs-content").css("margin-left")));
        var d = m($(".content-tabs").children().not(".J_menuTabs"));
        var e = $(".content-tabs").outerWidth(true) - d;
        var f = 0;
        if ($(".page-tabs-content").width() < e) {
            return false
        } else {
            var c = $(".J_menuTab:first");
            var b = 0;
            while ((b + $(c).outerWidth(true)) <= a) {
                b += $(c).outerWidth(true);
                c = $(c).next()
            }
            b = 0;
            if (m($(c).prevAll()) > e) {
                while ((b + $(c).outerWidth(true)) < (e) && c.length > 0) {
                    b += $(c).outerWidth(true);
                    c = $(c).prev()
                }
                f = m($(c).prevAll())
            }
        }
        $(".page-tabs-content").animate({
            marginLeft: 0 - f + "px"
        }, "fast")
    }
    function r() {
        var a = Math.abs(parseInt($(".page-tabs-content").css("margin-left")));
        var d = m($(".content-tabs").children().not(".J_menuTabs"));
        var e = $(".content-tabs").outerWidth(true) - d;
        var f = 0;
        if ($(".page-tabs-content").width() < e) {
            return false
        } else {
            var c = $(".J_menuTab:first");
            var b = 0;
            while ((b + $(c).outerWidth(true)) <= a) {
                b += $(c).outerWidth(true);
                c = $(c).next()
            }
            b = 0;
            while ((b + $(c).outerWidth(true)) < (e) && c.length > 0) {
                b += $(c).outerWidth(true);
                c = $(c).next()
            }
            f = m($(c).prevAll());
            if (f > 0) {
                $(".page-tabs-content").animate({
                    marginLeft: 0 - f + "px"
                }, "fast")
            }
        }
    }
    $(".J_menuItem").each(function(a) {
        if (!$(this).attr("data-index")) {
            $(this).attr("data-index", a)
        }
    });
    function q() {
        t();
        var a = $(this).attr("href"),
            c = $(this).data("index"),
            d = $.trim($(this).text()),
            e = true;
        if (a == undefined || $.trim(a).length == 0) {
            return false
        }
        $(".J_menuTab").each(function() {
            if ($(this).data("id") == a) {
                if (!$(this).hasClass("active")) {
                    $(this).addClass("active").siblings(".J_menuTab").removeClass("active");
                    l(this);
                    $(".J_mainContent .J_iframe").each(function() {
                        if ($(this).data("id") == a) {
                            $(this).show().siblings(".J_iframe").hide();
                            return false
                        }
                    })
                }
                e = false;
                return false
            }
        });
        if (e) {
            var f = '<a href="javascript:;" class="active J_menuTab" data-id="' + a + '">' + d + ' <i class="fa fa-times-circle"></i></a>';
            $(".J_menuTab").removeClass("active");
            var b = '<iframe class="J_iframe" name="iframe' + c + '" width="100%" height="100%" src="' + a + '" frameborder="0" data-id="' + a + '" seamless></iframe>';
            $(".J_mainContent").find("iframe.J_iframe").hide().parents(".J_mainContent").append(b);
            $(".J_menuTabs .page-tabs-content").append(f);
            l($(".J_menuTab.active"))
        }
        return false
    }
    $(".J_menuItem").on("click", q);
    document.body.onselectstart = document.body.oncontextmenu = function() {
        return false
    };
    $(".J_menuTabs").on("mousedown", ".J_menuTab", function(b) {
        var e = $(this);
        if (b.which === 3 && e.attr("data-id") !== "home") {
            var d = $("#content-main");
            $(".J_menuTab").removeClass("hover-right");
            e.addClass("hover-right");
            t();
            if ($("button.J_tabClose").attr("aria-expanded")) {
                $("button.J_tabClose").parent().removeClass("open");
                $("button.J_tabClose").attr("aria-expanded", "false")
            }
            var c = b.clientX - b.offsetX - $(".navbar-default").width();
            if (!e.hasClass("active")) {
                var a = '<ul class="J_menuTab_h_ul" style="left:' + c + 'px;"><li class="J_tabCloseAll"><a>关闭全部选项卡</a></li><li class="J_tabCloseOther"><a>关闭其他选项卡</a></li><li class="divider"></li><li class="J_tabShowNow"><a>显示当前选项卡</a></li></ul>'
            } else {
                var a = '<ul class="J_menuTab_h_ul" style="left:' + c + 'px;"><li class="J_tabCloseAll"><a>关闭全部选项卡</a></li><li class="J_tabCloseOther"><a>关闭其他选项卡</a></li><li class="divider"></li><li class="J_tabRefresh"><a>刷新当前选项卡</a></li></ul>'
            }
            d.append(a)
        }
        b.preventDefault();
        return false
    });
    function t() {
        if ($(".J_menuTab_h_ul").length > 0) {
            $(".J_menuTab_h_ul").remove()
        }
    }
    function x() {
        t();
        var c = $(this).parents(".J_menuTab").data("id");
        var d = $(this).parents(".J_menuTab").width();
        if ($(this).parents(".J_menuTab").hasClass("active")) {
            if ($(this).parents(".J_menuTab").next(".J_menuTab").size()) {
                var a = $(this).parents(".J_menuTab").next(".J_menuTab:eq(0)").data("id");
                $(this).parents(".J_menuTab").next(".J_menuTab:eq(0)").addClass("active");
                $(".J_mainContent .J_iframe").each(function() {
                    if ($(this).data("id") == a) {
                        $(this).show().siblings(".J_iframe").hide();
                        return false
                    }
                });
                var b = parseInt($(".page-tabs-content").css("margin-left"));
                if (b < 0) {
                    $(".page-tabs-content").animate({
                        marginLeft: (b + d) + "px"
                    }, "fast")
                }
                $(this).parents(".J_menuTab").remove();
                $(".J_mainContent .J_iframe").each(function() {
                    if ($(this).data("id") == c) {
                        $(this).remove();
                        return false
                    }
                })
            }
            if ($(this).parents(".J_menuTab").prev(".J_menuTab").size()) {
                var a = $(this).parents(".J_menuTab").prev(".J_menuTab:last").data("id");
                $(this).parents(".J_menuTab").prev(".J_menuTab:last").addClass("active");
                $(".J_mainContent .J_iframe").each(function() {
                    if ($(this).data("id") == a) {
                        $(this).show().siblings(".J_iframe").hide();
                        return false
                    }
                });
                $(this).parents(".J_menuTab").remove();
                $(".J_mainContent .J_iframe").each(function() {
                    if ($(this).data("id") == c) {
                        $(this).remove();
                        return false
                    }
                })
            }
        } else {
            $(this).parents(".J_menuTab").remove();
            $(".J_mainContent .J_iframe").each(function() {
                if ($(this).data("id") == c) {
                    $(this).remove();
                    return false
                }
            });
            l($(".J_menuTab.active"))
        }
        return false
    }
    $(".J_menuTabs").on("click", ".J_menuTab i", x);
    function w() {
        var a = $(this);
        if (a.parent("ul").hasClass("J_menuTab_h_ul")) {
            $(".page-tabs-content").children("[data-id]").not(":first").each(function(c, b) {
                if ($(b).hasClass("hover-right")) {
                    $(".J_iframe").hide();
                    $('.J_iframe[data-id="' + $(b).data("id") + '"]').show()
                } else {
                    $('.J_iframe[data-id="' + $(b).data("id") + '"]').remove();
                    $(b).remove()
                }
            });
            $(".J_menuTab").removeClass("active");
            $(".hover-right").addClass("active")
        } else {
            $(".page-tabs-content").children("[data-id]").not(":first").not(".active").each(function() {
                $('.J_iframe[data-id="' + $(this).data("id") + '"]').remove();
                $(this).remove()
            })
        }
        $(".page-tabs-content").css("margin-left", "0");
        t();
        return false
    }
    $("#page-wrapper").on("click", ".J_tabCloseOther", w);
    function v() {
        l($(".J_menuTab.active"))
    }
    $(".J_tabShowActive").on("click", v);
    function p() {
        $(".page-tabs-content").children("[data-id]").each(function(b, a) {
            if ($(a).hasClass("hover-right")) {
                $('.J_iframe[data-id="' + $(a).data("id") + '"]').show()
            } else {
                $('.J_iframe[data-id="' + $(a).data("id") + '"]').hide()
            }
        });
        $(".J_menuTab").removeClass("active");
        $(".hover-right").addClass("active");
        t();
        return false
    }
    $("#content-main").on("click", ".J_tabShowNow", p);
    function n() {
        if (!$(this).hasClass("active")) {
            var a = $(this).data("id");
            $(".J_mainContent .J_iframe").each(function() {
                if ($(this).data("id") == a) {
                    $(this).show().siblings(".J_iframe").hide();
                    return false
                }
            });
            $(this).addClass("active").siblings(".J_menuTab").removeClass("active");
            l(this)
        }
        $(".J_menuTab_h_ul").hide()
    }
    $(".J_menuTabs").on("click", ".J_menuTab", n);
    function o() {
        var a = $('.J_iframe[data-id="' + $(this).data("id") + '"]');
        var b = a.attr("src")
    }
    $("#page-wrapper").on("click", ".J_tabRefresh", function() {
        $(".J_iframe").each(function(a, c) {
            if ($(c).css("display") === "inline") {
                var b = $(c).attr("src");
                $(c).attr("src", b)
            }
        });
        t()
    });
    $(".J_menuTabs").on("dblclick", ".J_menuTab", o);
    $(".J_tabLeft").on("click", s);
    $(".J_tabRight").on("click", r);
    $("#page-wrapper").on("click", ".J_tabCloseAll", function() {
        $(".page-tabs-content").children("[data-id]").not(":first").each(function() {
            $('.J_iframe[data-id="' + $(this).data("id") + '"]').remove();
            $(this).remove()
        });
        $(".page-tabs-content").children("[data-id]:first").each(function() {
            $('.J_iframe[data-id="' + $(this).data("id") + '"]').show();
            $(this).addClass("active")
        });
        $(".page-tabs-content").css("margin-left", "0");
        t()
    })
});
